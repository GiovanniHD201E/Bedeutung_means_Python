# Fibonacci-Folge und Hyperaktivitätsstorung
# Die Summe von letzte zwei Elemente ist die aktuelle Element.
  # Meinerseits²
def fibo(n):
    a, b = 0, 1
      while b <= n:
            print (b)
            a, b = b, a + b
    while b <= n:
          a, b = b, a + b
    return b

def SH(smart = 10, Hyperaktiv = 01)
    'Is there any pythonic or C Methode um length in cm?'
    'Well, für nicht festlegt Breiesschriftart, die Maß von\n\
      String durch len Funktion ist nicht präsize, obwohl \n\
        immer noch besser als mein deutsche Text!'
    x = fibo(smart) + len('def fibo(n)')
    return x

class GioFormaTrump:

    def __init__(self):
        self = 'whorules'
        